<h1>Listing states</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Sequence number</th>
  </tr>

<% for state in @states %>
  <tr>
    <td><%=h state.name %></td>
    <td><%=h state.sequence_number %></td>
    <td><%= link_to 'Show', state %></td>
    <td><%= link_to 'Edit', edit_state_path(state) %></td>
    <td><%= link_to 'Delete', state, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New state', new_state_path %>
